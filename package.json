{
  "name": "profile-landing",
  "version": "1.0.0",
  "private": true,
  "description": "Mashong Official Portal",
  "scripts": {
    "build": "echo 'No build required for static assets'",
    "deploy": "wrangler deploy"
  },
  "engines": {
    "node": ">=22.0.0"
  },
  "devDependencies": {
    "wrangler": "^4.135.0"
  }
}
